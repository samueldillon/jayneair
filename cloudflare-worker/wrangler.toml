name = "jayneair-feed-proxy"
main = "src/index.ts"
compatibility_date = "2026-08-01"

# Custom domain for the Worker, once jayneair.uk is a Cloudflare zone.
# Comment out (or remove) to fall back to the free *.workers.dev URL.
routes = [
  { pattern = "feed.jayneair.uk", custom_domain = true }
]
